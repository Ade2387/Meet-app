<H1>Here are the available slot for the meeting: <%= @event.description %></H1>

<ul>
  <% @event.slots.each do |slot| %>
    <li>start: <%= slot.start_at %>
        - end: <%= slot.end_at %>
        - status: <%= slot.status %>
  <% end %>
</ul>
